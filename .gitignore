# Certificate files - contain sensitive private keys
*.pfx
*.p12
*.cer
*.pem
*.key

# Base64 encoded certificate data
*-Base64.txt

# Configuration files are safe to commit (secrets are in Key Vault)
# Only ignore if someone creates a local override
config/*.local.psd1

# Environment and credential files
.env
*.env
.env.*
credentials.json
secrets.*

# Deployment state files
Autopilot-DeploymentState.xml
*-DeploymentState.xml

# Claude Code / Claude Sentient
.claude/
profiles/
rules/
templates/
tmpclaude-*

# PowerShell logs (may contain sensitive info)
*.log
Autopilot-*.json

# Windows system files
Thumbs.db
Desktop.ini

# IDE files
.vscode/
.idea/
*.suo
*.user
